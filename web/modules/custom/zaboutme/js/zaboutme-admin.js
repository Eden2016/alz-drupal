/**
 * @file
 * Defines Javascript behaviors for the zaboutme module.
 */

(function ($, Drupal, drupalSettings) {

  'use strict';

  Drupal.behaviors.zaboutmeInit = {
    attach: function (context) {
      var $context = $(context);
      
    }
  };

})(jQuery, Drupal, drupalSettings);
